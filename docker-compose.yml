version: "3.9"

services:

  backend:
    build: ./Backend

    ports:
      - "4000:4000"

    restart: unless-stopped


  frontend:
    build: ./Frontend

    ports:
      - "5173:5173"

    depends_on:
      - backend

    restart: unless-stopped